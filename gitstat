#!/bin/bash
set -o errexit
set -o nounset

# Git status van alle repo's in repo.list.

while read -r repo; do
    base=$(basename "$repo")
    printf "$(tput bold)%s$(tput sgr0)\n" "$base"
    eval cd "$repo" || exit 1
    git status
done < /home/karel/bin/repo.list
