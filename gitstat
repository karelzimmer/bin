#!/bin/bash

# Git status van alle repo's.

readonly -a REPO=(
    'bin'
    'kz-deb'
    'kz-docs'
    'kz-scripts'
    'kz-uploads'
    'work'
    )

for repo in "${REPO[@]}"; do
    printf "$(tput bold)%s$(tput sgr0)\n" "$repo"
    cd "$HOME/$repo" || exit 1
    git status
done
